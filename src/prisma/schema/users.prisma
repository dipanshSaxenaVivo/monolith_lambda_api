model users {
  id          String  @id @default(auto()) @map("_id") @db.ObjectId
  email       String
  name        String
  phoneNumber Int
  password    String?

  @@unique([email,phoneNumber])
  @@fulltext([name])
}
